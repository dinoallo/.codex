interface:
  display_name: "Proxmox Ensure VM"
  short_description: "Provision Proxmox VM fleets with control nodes"
  default_prompt: "Use $proxmox-ensure-vm to provision or adapt the bundled OpenTofu module for a Proxmox control/master/worker VM fleet."
