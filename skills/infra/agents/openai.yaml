interface:
  display_name: "Proxmox Ensure VM"
  short_description: "Provision a Proxmox VM fleet with OpenTofu"
  default_prompt: "Use $proxmox-ensure-vm to provision or adapt the bundled OpenTofu module for a Proxmox master/worker VM fleet."
